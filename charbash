if [[ $1 == "stats" ]]; then
  cat ./char_info/basic_stats
  fi

if [[ $1 == "h" ]]; then
  hp=$(cat ./char_info/hp)
  echo $hp
  if [[ $2 != "" ]]; then
    echo "Adding $2 HP to player"

  fi

if [ -z $1 ];
  then cat ./charbash_info
  fi
